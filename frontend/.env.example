NEXT_PUBLIC_API_URL=http://localhost:8000

# Phase 7 default: the new sessions flow is on. Flip to "false" to roll
# back to the legacy /task/chat flow without a deploy. Phase 8 removes
# the legacy code path entirely; this flag goes away at that point.
NEXT_PUBLIC_USE_NEW_SESSION_FLOW=true
