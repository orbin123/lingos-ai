name: frontend

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - ".github/workflows/frontend.yml"
  # No paths filter on PRs: `ci` is a REQUIRED status check under branch
  # protection (strict). A path-filtered required check never reports on an
  # unrelated PR, which wedges the merge forever (see POLISHED plan, Appendix B).
  pull_request:

concurrency:
  group: frontend-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    working-directory: frontend

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20 # Next 16 / React 19 need Node >= 20.9
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: ESLint
        run: npm run lint
      - name: Type check
        run: npx tsc --noEmit
      - name: Unit + integration tests (Vitest + coverage)
        run: npm run test:coverage
      - name: Build verification
        run: npm run build
        env:
          # Dummy; the build must not require a live backend.
          NEXT_PUBLIC_API_URL: http://localhost:8000
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: frontend-coverage
          path: frontend/coverage/
