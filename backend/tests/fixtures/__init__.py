"""Shared data builders for tests (curriculum/archetype seeds, golden payloads).

Populated in Phase 3 of the testing refactor (see docs/testing-strategy.md);
this placeholder reserves the package so the tree is importable now.
"""
