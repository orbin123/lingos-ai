# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
.claude/

# Local docs (internal notes, walkthroughs, ad-hoc plans — not for public)
/docs/*
# Reviewed production migration plan; keep this one report with the repository.
!/docs/AZURE_ZERO_COST_MIGRATION.md
!/docs/AZURE_AGENT_MIGRATION_PLAYBOOK.md
!/docs/AZURE_CICD_RUNBOOK.md
!/docs/AZURE_DATA_MIGRATION_REHEARSAL.md
!/docs/AZURE_FRESH_START_ADMIN_BOOTSTRAP.md
!/docs/AZURE_POSTGRES_MANAGED_IDENTITY.md
!/docs/AZURE_VM_HOST_BOOTSTRAP.md
!/docs/AZURE_DEPLOYMENT_STATUS.md
!/docs/AZURE_PHASE0_OWNER_CHECKLIST.md
!/docs/AZURE_EPHEMERAL_BILLING_RUNBOOK.md

# Azure go-live plan and its progress log. These are the two files a new
# session reads to pick the work up, so they belong with the repository.
!/docs/azure/
!/docs/azure/README.md
!/docs/azure/MASTER_PLAN.md
!/docs/azure/AGENT_WORK.md
!/docs/azure/LIVE_STATUS.md

# macOS (IMPORTANT for you)
.DS_Store

# Node (for frontend later)
node_modules/
.next/
dist/

# Logs
*.log

# Database
*.db
*.sqlite3
# Postgres dumps (may contain real user data — never commit)
backup*.sql
*.dump

# Root-level ad-hoc/ops scripts (DR drills, dev shortcut) — kept local, not online.
# Anchored with a leading slash so it does NOT also match backend/scripts/.
# Matches the contents rather than the directory itself, so git descends into it
# and the reviewed Azure operator scripts below can be re-included.
/scripts/*
!/scripts/build-prod-env.sh
!/scripts/_azure-common.sh
!/scripts/azure-up.sh
!/scripts/azure-down.sh
!/scripts/azure-status.sh
!/scripts/azure-auto-down.sh

# Test Files
backend/scratch
# Ignore scripts/ contents (ad-hoc DB tools), but git must descend into the dir
# so the negations below can re-include the test/CI-contract scripts. Excluding
# the dir itself (`backend/scripts`) would make those negations a no-op.
backend/scripts/*
# …but these are part of the test/CI contract, not ad-hoc tooling:
!backend/scripts/__init__.py
!backend/scripts/export_openapi.py
!backend/scripts/run_evals.py
!backend/scripts/calibrate_judge.py
!backend/scripts/golden_set.json
# Production entrypoint — part of the deploy contract, must be committed:
!backend/scripts/start-prod.sh
# DB seeders run by the deploy pipeline (CD) — must ship in the image, not ad-hoc:
!backend/scripts/seed_curriculum.py
!backend/scripts/seed_ielts_challenge.py
!backend/scripts/seed_a2z_challenge.py
!backend/scripts/azure_data_rehearsal.py
!backend/scripts/cleanup_media_cache.py
!backend/scripts/bootstrap_azure_postgres.py
!backend/scripts/bootstrap_fresh_admin.py
!backend/scripts/check_external_services.py

# Local archive of deprecated files (kept locally, not committed)
backend/.archive/

# Coverage (pytest-cov / coverage.py)
.coverage
.coverage.*
htmlcov/
coverage.xml

# TTS audio cache (regenerated on demand from OpenAI)
backend/app/ai/tts/_cache/

# STT transcript cache (regenerated on demand from OpenAI)
backend/app/ai/stt/_cache/

# Learner speaking uploads (local dev only)
backend/app/ai/stt/_learner_audio/

# Pronunciation assessment cache (JSON only, never raw user audio)
backend/app/ai/pronunciation/_cache/

# Image-generation cache (regenerated on demand from OpenAI)
backend/app/ai/imagegen/_cache/
